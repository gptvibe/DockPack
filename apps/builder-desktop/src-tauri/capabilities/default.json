{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for DockPack Builder.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
